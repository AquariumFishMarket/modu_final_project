import styled from "styled-components";

export const FollowListContainer = styled.ul`
  list-style: none;
  margin: 0;
  padding: 0 1.6rem;
  width: 100%;
`;

export const EmptyState = styled.p`
  padding: 2.4rem 0;
  text-align: center;
  color: var(--color-gray-dark);
  font-size: var(--font-size-md);
`;
